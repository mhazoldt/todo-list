<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>To Do List</title>
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <!-- Optional theme -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css" integrity="sha384-rHyoN1iRsVXV4nD0JutlnGaslCJuC7uwjduW9SVrLvRYooPp2bWYgmgJQIXwl/Sp" crossorigin="anonymous">
    <style type="text/css">
      .todo-list {
        list-style-type: none;
        padding-left: 0.5em;
      }

      .todo-list li {
        margin-bottom: 1rem;
      }
      .done {
        text-decoration: line-through;
      }
    </style>
  </head>
  <body>

    <div class="container">
      <div class="row">
        <div class="col-xs-12">
          <!-- TODO write your HTML/Mustache here -->
          <h1>ToDo</h1>
           <form action="/" method="post" id="checkboxForm">
            <div id="todoItemsDiv">
              {{#todoItems}}
                {{^isChecked}}
                  <div><input class="formCheckbox" type="checkbox" value="{{description}}" onchange="convertCheckboxData()"> {{description}}</div>            
                {{/isChecked}}
              {{/todoItems}}
            </div>
            <br>
            <button onclick="convertCheckboxData()">Add</button><input name="addCheckboxText" type="text" id="#addToDoItem" style="width: 400px;">
            <h1>Complete</h1>
            <div>
              {{#todoItems}}
                  {{#isChecked}}
                    <div class="done"><input class="formCheckbox" type="checkbox" value="{{description}}" onchange="convertCheckboxData()" checked> {{description}}</div>
                  {{/isChecked}}
              {{/todoItems}}
            </div>
            <input type="hidden" name="checkboxData">
          </form>
        </div>
      </div>
    </div>

    <script src="./main.js"></script>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
  </body>
</html>

<!-- Latest compiled and minified JavaScript -->
